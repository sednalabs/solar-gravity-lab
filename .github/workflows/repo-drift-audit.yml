name: repo-drift-audit

on:
  schedule:
    - cron: '0 4 * * 1'
  workflow_dispatch:
    inputs:
      local_ref:
        description: Local baseline reference
        required: true
        default: origin/main
        type: string
      remote_ref:
        description: Preferred baseline reference (for example auto:last-release-tag, tag:v0.1.0-alpha.13, origin/main)
        required: false
        default: auto:last-release-tag
        type: string
      fallback_remote_ref:
        description: Fallback reference if the preferred remote is unavailable
        required: false
        default: origin/main
        type: string
      strict:
        description: Treat drift as a hard failure
        required: false
        default: false
        type: boolean
      code_only:
        description: Include only likely code paths in output/decision
        required: false
        default: true
        type: boolean
      ignore_paths:
        description: Comma-separated paths to ignore in file delta output
        required: false
        type: string

permissions:
  contents: read

jobs:
  drift:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout history
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Compare origin/main against baseline release (or fallback) remote
        id: audit
        shell: bash
        run: |
          set -euo pipefail
          local_ref="${{ inputs.local_ref }}"
          remote_ref="${{ inputs.remote_ref }}"
          fallback_remote_ref="${{ inputs.fallback_remote_ref }}"
          strict="${{ inputs.strict }}"
          code_only="${{ inputs.code_only }}"
          ignore_paths="${{ inputs.ignore_paths }}"

          local_ref=${local_ref:-origin/main}
          remote_ref=${remote_ref:-auto:last-release-tag}
          fallback_remote_ref=${fallback_remote_ref:-origin/main}
          ignore_paths=${ignore_paths:-}

          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git fetch --tags --force --prune origin 'refs/tags/v*'

          args=(
            --local-ref "$local_ref"
            --remote-ref "$remote_ref"
            --fallback-remote-ref "$fallback_remote_ref"
            --json drift_audit.json
            --markdown drift_audit.md
          )

          if [[ "$code_only" == "true" ]]; then
            args+=(--code-only)
          fi

          if [[ -n "$ignore_paths" ]]; then
            args+=(--ignore-paths "$ignore_paths")
          fi

          if [[ "$strict" == "true" ]]; then
            args+=(--strict)
          fi

          python3 .github/scripts/compare_upstream_main.py "${args[@]}"

      - name: Upload drift artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: upstream-drift-audit
          path: |
            drift_audit.json
            drift_audit.md

      - name: Emit audit summary
        if: always()
        run: |
          echo "## repo-drift-audit" >> "$GITHUB_STEP_SUMMARY"
          cat drift_audit.md >> "$GITHUB_STEP_SUMMARY"
